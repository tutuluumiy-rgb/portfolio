# 交给其他 Agent 的统一资料模板说明

## 文档目的

这份文档专门说明交给其他 Agent 整理项目资料时，每个字段应该填写什么。

每个字段都明确三件事：

1. 内容含义：这个字段回答什么问题。
2. 填写要求：内容应该写到什么程度，不能写什么。
3. 输出格式：Agent 最后应该用什么形式返回。

其他 Agent 必须按照本文档的字段顺序输出。没有资料的字段不要猜测，统一写“待确认”或“暂无证据”。

本模板不包含：

- 角色、实际工作、团队分工等字段。
- 产品演示视频提示词、旁白提示词或生成脚本。
- 没有真实依据的指标、用户数量、效率提升比例或结果。

## 一、统一输出规则

### 1.1 内容来源

所有文字都必须来自项目真实资料，包括：

- 已有产品和代码。
- 产品截图和视频。
- 真实交互过程。
- 测试记录。
- 用户反馈。
- 失败样例。
- 设计过程中的明确决策记录。

如果 Agent 根据资料进行了推断，必须标注为“推断”。如果资料无法证明，必须标注为“待确认”。

### 1.2 文字表达规则

- 先写用户任务，再写产品机制。
- 先写产品判断，再写技术实现。
- 每段只表达一个核心结论。
- 不把技术名词直接当成产品价值。
- 不把“已经实现”当成案例证据。
- 不编造精确数据。
- 不用空泛的“提升体验”“提高效率”代替具体结果。
- 不同字段不要重复粘贴同一段话。

### 1.3 输出形式规则

| 内容类型 | 输出格式 |
|---|---|
| 单个判断 | 一句话 |
| 项目摘要 | 一段话，建议 2 到 3 句 |
| 关键词 | 2 到 4 个，用斜杠或顿号分隔 |
| 过程说明 | 按先后顺序写，必要时使用编号 |
| 证据 | 列表，每条对应一个可核对材料 |
| 文件材料 | 文件路径加材料说明 |
| 逻辑图 | Mermaid flowchart 源码 |
| 节点解释 | 一个节点一个固定字段组 |
| 章节内容 | 固定六章，每章使用相同字段组 |
| 不确定内容 | 写“待确认”，并说明缺少什么 |

## 二、项目定位字段

项目定位用于让页面和面试官快速知道“这是什么项目”。这些内容通常对应项目详情页顶部的项目标签、项目名称和基础信息。

### 2.1 项目名称

字段名：name

| 项目 | 说明 |
|---|---|
| 内容含义 | 项目的正式名称，回答“这个项目叫什么” |
| 填写要求 | 名称要稳定、简洁、方便复述，并与首页项目卡片保持一致。不要把一串技术栈写进名称 |
| 输出格式 | 一行文本。若有中英文名称，按“中文名 / English Name”输出 |

示例格式：

~~~text
项目名称：建筑生图工作台 / Architecture Image Studio
~~~

### 2.2 项目类别

字段名：label

| 项目 | 说明 |
|---|---|
| 内容含义 | 项目所属的产品方向或问题领域，回答“它属于哪个方向” |
| 填写要求 | 只写分类，不在这里展开项目价值。可以包含语音 AI、Agent、RAG、生成式 AI、评测等方向 |
| 输出格式 | 一行文本，建议使用 1 到 2 个方向，用“·”分隔 |

示例格式：

~~~text
项目类别：Voice AI · Agent
~~~

### 2.3 目标用户或使用场景

字段名：users 的资料来源

| 项目 | 说明 |
|---|---|
| 内容含义 | 谁使用产品，或者产品主要发生在哪个工作场景 |
| 填写要求 | 既要写用户类型，也要尽量补充使用情境。不要只写“所有人”或“企业用户” |
| 输出格式 | 一行文本，格式为“用户类型 / 主要场景” |

示例格式：

~~~text
目标用户或使用场景：开发者 / 需要托付复杂编码任务的 Agent 重度用户
~~~

### 2.4 核心任务

字段名：core task

| 项目 | 说明 |
|---|---|
| 内容含义 | 用户希望通过产品完成的具体任务，回答“用户到底要做什么” |
| 填写要求 | 写清输入、动作和结果。不能只写“使用 AI”“提高效率” |
| 输出格式 | 一句话，建议使用“用户从【输入】经过【过程】，得到【结果】” |

示例格式：

~~~text
核心任务：用户输入一个复杂编码目标，Agent 经过规划、工具执行和验证后交付可运行的结果。
~~~

### 2.5 当前产品状态

字段名：status

| 项目 | 说明 |
|---|---|
| 内容含义 | 项目现在处于什么状态，回答“它做到哪一步了” |
| 填写要求 | 只能根据真实情况填写，例如概念验证、可用原型、持续迭代、已上线、内部使用。不要把未来计划写成已完成 |
| 输出格式 | 一行短文本，建议不超过 20 个字 |

示例格式：

~~~text
当前产品状态：可用原型 / 持续迭代
~~~

### 2.6 项目重点

字段名：focus

| 项目 | 说明 |
|---|---|
| 内容含义 | 这个项目最值得面试官关注的产品方向 |
| 填写要求 | 写 2 到 4 个关键词，关键词之间应有明确关系。不要堆砌所有技术名词 |
| 输出格式 | 一行文本，用“·”或“/”分隔 |

示例格式：

~~~text
项目重点：可见执行 · 用户控制 · 失败恢复
~~~

### 2.7 产品范围或任务边界

字段名：scope，属于可选补充字段

| 项目 | 说明 |
|---|---|
| 内容含义 | 产品明确解决什么，以及暂时不解决什么 |
| 填写要求 | 用边界防止项目被理解得过大。只写真实的当前范围，不要写愿景口号 |
| 输出格式 | 两句话以内，分别写“解决范围”和“不包含范围” |

示例格式：

~~~text
产品范围：
解决：支持复杂任务的规划、工具执行、过程反馈和结果验证。
不包含：暂不覆盖完全无人监督的长期自主运行。
~~~

## 三、项目概览文案字段

这组内容用于详情页顶部的核心文案。大标题、副标题和摘要必须分别承担不同作用，不能写成同一段话的三个版本。

### 3.1 大标题

字段名：headline

| 项目 | 说明 |
|---|---|
| 内容含义 | 项目想为用户带来的变化，回答“这个项目最值得记住的价值是什么” |
| 填写要求 | 表达用户结果、产品变化或核心产品判断。不要写项目名称、技术栈或功能清单 |
| 输出格式 | 一句话，建议 12 到 28 个中文字符，结尾可以使用句号 |

推荐句式：

~~~text
让【用户任务】变得【新的状态】。
把【原本困难的事情】变成【可见、可控、可验证的事情】。
让【复杂任务】能够被【可靠地完成】。
~~~

### 3.2 副标题

字段名：subheadline

| 项目 | 说明 |
|---|---|
| 内容含义 | 实现大标题所表达价值的关键产品机制，回答“这个变化靠什么实现” |
| 填写要求 | 至少写出 2 个关键机制，并说明它们如何共同形成产品闭环。不要只列技术组件 |
| 输出格式 | 一句话，建议 20 到 45 个中文字符 |

推荐句式：

~~~text
通过【机制 1】、【机制 2】和【机制 3】，让【用户结果】可见、可控、可验证。
~~~

### 3.3 项目摘要

字段名：summary

| 项目 | 说明 |
|---|---|
| 内容含义 | 对产品形态、用户、核心任务和当前状态做完整但简短的说明 |
| 填写要求 | 需要让第一次阅读的人知道产品是什么、为谁服务、解决什么任务、现在做到什么程度 |
| 输出格式 | 一段话，建议 2 到 3 句，约 60 到 120 个中文字符 |

推荐结构：

~~~text
面向【用户/场景】的【产品形态】，帮助用户完成【核心任务】。
产品通过【关键能力】改善【原有问题】，当前状态为【真实状态】。
~~~

### 3.4 顶部信息卡片 Users

字段名：users

| 项目 | 说明 |
|---|---|
| 内容含义 | 页面顶部信息卡片中的目标用户或使用场景 |
| 填写要求 | 和项目定位中的目标用户保持一致，可以比定位字段更短。不要写角色或团队信息 |
| 输出格式 | 一行文本，建议使用“用户类型 / 场景” |

### 3.5 顶部信息卡片 Status

字段名：status

| 项目 | 说明 |
|---|---|
| 内容含义 | 页面顶部信息卡片中的真实产品状态 |
| 填写要求 | 和项目定位中的当前状态保持一致。不能用“成熟”“领先”等无法核对的词 |
| 输出格式 | 一行短文本，建议使用“状态 / 阶段” |

### 3.6 顶部信息卡片 Focus

字段名：focus

| 项目 | 说明 |
|---|---|
| 内容含义 | 页面顶部信息卡片中的项目重点 |
| 填写要求 | 和项目定位中的项目重点保持一致，最多保留 2 到 4 个关键词 |
| 输出格式 | 一行文本，用“·”或“/”分隔 |

### 3.7 顶部信息卡片 Scope

字段名：scope，可选

| 项目 | 说明 |
|---|---|
| 内容含义 | 页面如果预留产品范围信息位，这里说明产品边界 |
| 填写要求 | 只写明确范围和明确限制，不把角色、完成方式或个人经历放在这里 |
| 输出格式 | 一行或两行，格式为“覆盖：……；暂不覆盖：……” |

## 四、产品演示材料字段

这一组内容用于说明产品演示文件本身。只提供真实视频或截图信息，不要求 Agent 生成视频提示词。

### 4.1 视频文件路径

字段名：video

| 项目 | 说明 |
|---|---|
| 内容含义 | 详情页第一段产品演示视频的位置 |
| 填写要求 | 必须是项目中真实存在的文件。路径要能被网页访问，不要只写电脑上的临时路径 |
| 输出格式 | 一行相对项目路径，推荐使用正斜杠 |

示例格式：

~~~text
视频 1 路径：assets/videos/project-demo-1.mp4
~~~

### 4.2 第二段视频文件路径

字段名：video2，可选

| 项目 | 说明 |
|---|---|
| 内容含义 | 详情页第二段产品演示视频的位置 |
| 填写要求 | 只有确实需要展示第二条独立流程时才填写。不要把同一个视频重复填两次 |
| 输出格式 | 一行相对项目路径；没有时写“无” |

示例格式：

~~~text
视频 2 路径：assets/videos/project-demo-2.mp4
~~~

### 4.3 视频展示任务

字段名：demo task，属于资料说明字段

| 项目 | 说明 |
|---|---|
| 内容含义 | 这段视频正在展示用户完成什么任务 |
| 填写要求 | 写清用户输入、产品主要动作和最终结果，不要只写“产品演示” |
| 输出格式 | 一句话，建议使用“用户输入……，产品……，最后……” |

### 4.4 视频标题

字段名：demo title，属于资料说明字段

| 项目 | 说明 |
|---|---|
| 内容含义 | 面试官看到视频时应该知道这段视频的主题 |
| 填写要求 | 直接命名视频展示的任务或能力，避免使用“完整演示”“功能介绍”等空标题 |
| 输出格式 | 一行短标题，建议不超过 20 个字 |

### 4.5 视频中的关键结果

字段名：demo outcome，属于资料说明字段

| 项目 | 说明 |
|---|---|
| 内容含义 | 视频最后证明产品完成了什么 |
| 填写要求 | 写可观察结果，例如生成了什么、保存了什么、用户确认了什么、系统恢复了什么 |
| 输出格式 | 一句话，不能写没有出现在视频里的结果 |

### 4.6 截图文件路径

字段名：screenshots

| 项目 | 说明 |
|---|---|
| 内容含义 | 用于支持页面说明或节点证据的产品截图 |
| 填写要求 | 每张截图都要能对应一个具体状态或动作，不能只提交一批没有说明的图片 |
| 输出格式 | 列表，每条包含“路径 + 截图对应状态” |

示例格式：

~~~text
截图：
- assets/images/agent-panel.png：Agent 正在流式输出并等待用户确认
- assets/images/tool-result.png：工具执行完成后结果回传到面板
~~~

## 五、产品逻辑图的定义依据字段

这一组内容先于 Mermaid 图。Agent 必须先说明逻辑图根据什么定义，再画图。

### 5.1 项目最核心的特点

字段名：core characteristic

| 项目 | 说明 |
|---|---|
| 内容含义 | 项目最有辨识度、最值得展开的产品特点 |
| 填写要求 | 要说明它解决了什么问题，以及没有这个特点时产品会退化成什么。不能只写“用了 Agent”或“用了大模型” |
| 输出格式 | 一段话，建议 2 到 3 句，包含“特点、解决的问题、与普通产品的区别” |

推荐格式：

~~~text
核心特点：
【产品】的核心特点是【特点】。
它解决【用户问题】；如果没有这个特点，产品会退化为【普通形态】。
~~~

### 5.2 如果没有这个特点，产品会退化成什么

字段名：degraded form

| 项目 | 说明 |
|---|---|
| 内容含义 | 用来检验核心特点是否真的成立 |
| 填写要求 | 要写一个具体的普通产品形态，而不是写“体验变差” |
| 输出格式 | 一句话，使用“没有该特点时，产品会变成……” |

示例格式：

~~~text
没有该特点时，产品会变成：用户手动填写参数、逐个点击工具、只能等待最终结果的普通表单或聊天框。
~~~

### 5.3 用户输入

字段名：user input

| 项目 | 说明 |
|---|---|
| 内容含义 | 用户启动任务时提供给产品的信息 |
| 填写要求 | 写明输入的类型、来源和可能的不完整状态，例如语音、文本、参考图、历史上下文、文件或画布引用 |
| 输出格式 | 列表，建议 1 到 4 项 |

### 5.4 用户任务终点

字段名：task outcome

| 项目 | 说明 |
|---|---|
| 内容含义 | 用户认为任务完成时得到的结果 |
| 填写要求 | 必须是用户能够观察或使用的结果，不要把“API 返回成功”当成任务终点 |
| 输出格式 | 一句话，格式为“用户最终得到……” |

### 5.5 用户遇到的主要问题

字段名：user problem

| 项目 | 说明 |
|---|---|
| 内容含义 | 用户为什么需要这个产品，当前流程卡在哪里 |
| 填写要求 | 写具体任务中的阻力、风险、等待、歧义、不可控或无法复核问题 |
| 输出格式 | 3 到 5 条列表，每条描述一个问题和它造成的影响 |

### 5.6 必须存在的产品机制

字段名：necessary mechanisms

| 项目 | 说明 |
|---|---|
| 内容含义 | 为解决核心问题，产品流程中不能缺少的机制 |
| 填写要求 | 只保留支撑核心特点的机制。每项要说明它解决什么问题，不要把所有后端服务全部列出 |
| 输出格式 | 列表，每条使用“机制名称：解决的问题” |

### 5.7 系统可以自动完成的动作

字段名：automated actions

| 项目 | 说明 |
|---|---|
| 内容含义 | 产品可以替用户完成的判断、整理、检索、规划、调用或生成动作 |
| 填写要求 | 写清自动化的边界，不要默认所有动作都可以交给系统 |
| 输出格式 | 列表，每条使用“动作 + 输入 + 输出” |

### 5.8 必须由用户确认或控制的动作

字段名：human control

| 项目 | 说明 |
|---|---|
| 内容含义 | 用户需要看到、确认、修改、中断或选择的环节 |
| 填写要求 | 说明为什么这里不能完全自动化，以及用户控制会改变什么 |
| 输出格式 | 列表，每条使用“控制点：用户动作；原因：……” |

### 5.9 失败、回退或重试方式

字段名：failure recovery

| 项目 | 说明 |
|---|---|
| 内容含义 | 当系统没有得到正确结果时，流程如何继续 |
| 填写要求 | 至少说明失败表现、处理动作和回到哪个节点。没有恢复能力时要直接写明 |
| 输出格式 | 列表或编号流程，格式为“失败条件 → 处理方式 → 回到节点” |

### 5.10 最终结果如何反馈给用户

字段名：result feedback

| 项目 | 说明 |
|---|---|
| 内容含义 | 产品如何让用户知道系统完成了什么、当前状态是什么、是否需要继续操作 |
| 填写要求 | 写可见的界面状态、消息、结果卡片、引用、日志、文件、画布变化或通知 |
| 输出格式 | 一句话或 3 条以内列表 |

## 六、产品逻辑图字段

### 6.1 逻辑图标题

字段名：logicTitle

| 项目 | 说明 |
|---|---|
| 内容含义 | 这张图要解释的核心特点和主流程 |
| 填写要求 | 标题中要出现用户任务、核心行为或最终结果。不能只写“系统架构图” |
| 输出格式 | 一句话，建议 15 到 35 个中文字符 |

推荐格式：

~~~text
从【用户输入】，到【关键结果】的【产品闭环】。
【核心特点】如何支撑【核心用户任务】。
~~~

### 6.2 逻辑图说明

字段名：logicDescription

| 项目 | 说明 |
|---|---|
| 内容含义 | 解释这张图依据哪个特点定义，展示哪条主流程，以及图的范围边界 |
| 填写要求 | 必须明确写出核心特点、输入、关键机制、最终结果和不展开的内容 |
| 输出格式 | 一段话，建议 2 到 3 句 |

固定格式：

~~~text
本图围绕“【项目核心特点】”展开，展示用户如何从【输入任务】经过【关键机制】，最终得到【用户结果】。图中只保留支撑这一特点的产品机制、系统模块、用户控制和反馈关系，不展开无关的部署细节。
~~~

### 6.3 图形格式

字段名：diagram format

| 项目 | 说明 |
|---|---|
| 内容含义 | 说明逻辑图使用的绘图语言和图形类型 |
| 填写要求 | 当前项目使用 Mermaid 流程图，不要提交图片代替源码 |
| 输出格式 | 固定写“Mermaid flowchart” |

### 6.4 流程方向

字段名：direction

| 项目 | 说明 |
|---|---|
| 内容含义 | Mermaid 图中主流程的阅读方向 |
| 填写要求 | 任务阶段较多时使用 TB，从上到下；输入到输出关系明显时可以使用 LR，从左到右 |
| 输出格式 | 只写 TB 或 LR，并补充一句选择原因 |

示例格式：

~~~text
流程方向：TB
选择原因：流程包含用户输入、Agent 决策、工具执行、用户确认和反馈回路，按阶段从上到下阅读更清晰。
~~~

### 6.5 Mermaid 源码

字段名：mermaid

| 项目 | 说明 |
|---|---|
| 内容含义 | 页面实际渲染的产品逻辑图源码 |
| 填写要求 | 必须能表达从输入到结果的主流程，并保留关键控制、反馈、失败和回退关系 |
| 输出格式 | Mermaid flowchart 源码，使用代码块输出 |

必须满足：

- 使用 flowchart TB 或 flowchart LR。
- 关键节点使用唯一 ID。
- 节点 ID 要和 logicNotes 中的键一致。
- 每个需要点击解释的节点都要有 click 绑定。
- 主流程使用实线。
- 用户控制、反馈、失败回退和观察关系使用虚线。
- 建议保留 6 到 12 个核心节点。
- 节点名称使用产品语言，不要直接堆叠类名、文件名和接口名。
- 如果节点文字含有括号、冒号、斜杠等符号，使用引号包裹显示文字。

当前页面的交互格式：

~~~text
flowchart TB
    U["用户输入"] --> A["Agent 处理"]
    A --> R["结果输出"]
    R -. "用户修改" .-> A

    click U call handleMermaidNode() "查看用户输入"
    click A call handleMermaidNode() "查看 Agent 处理"
    click R call handleMermaidNode() "查看结果输出"
~~~

### 6.6 初始选中节点

字段名：logicInitial

| 项目 | 说明 |
|---|---|
| 内容含义 | 用户打开逻辑图时，节点解释面板默认展示哪个节点 |
| 填写要求 | 应选择最能代表项目核心特点的节点，不要默认选择无关的基础设施节点 |
| 输出格式 | 一个 Mermaid 节点 ID，且必须存在于 logicNotes |

示例格式：

~~~text
初始选中节点：A
选择原因：该节点最直接体现项目的核心产品机制。
~~~

## 七、逻辑图节点字段

逻辑图中的每一个关键节点都必须配一组解释。节点数量和 Mermaid 中可点击解释的节点保持一致。

### 7.1 节点编号

| 项目 | 说明 |
|---|---|
| 内容含义 | 方便 Agent 和后续页面开发核对节点 |
| 填写要求 | 编号必须唯一，顺序可以按照主流程排列 |
| 输出格式 | “节点 1、节点 2……” |

### 7.2 节点分类

字段名：logicNotes[nodeId].label

| 项目 | 说明 |
|---|---|
| 内容含义 | 说明该节点属于用户动作、产品界面、Agent 决策、工具执行、数据状态、用户控制或反馈等哪一类 |
| 填写要求 | 分类要帮助读者理解节点责任，不要直接使用代码类名 |
| 输出格式 | 一行短标签，建议使用“类别 / 子类别” |

示例格式：

~~~text
节点分类：Agent / Planning
~~~

### 7.3 节点名称

字段名：logicNotes[nodeId].title

| 项目 | 说明 |
|---|---|
| 内容含义 | 用短名称说明这一步在产品中具体做什么 |
| 填写要求 | 一个节点只表达一个主要动作或责任。名称要和 Mermaid 节点保持一致或高度一致 |
| 输出格式 | 一行短标题，建议 6 到 16 个中文字符 |

不推荐：

~~~text
调用 ModelService.handleRequestAndPersistContext()
~~~

推荐：

~~~text
任务拆解与节点规划
~~~

### 7.4 节点说明

字段名：logicNotes[nodeId].description

| 项目 | 说明 |
|---|---|
| 内容含义 | 说明节点接收什么、完成什么动作、输出什么结果 |
| 填写要求 | 要写产品行为，不要只写技术实现。必要时说明下一步由谁继续处理 |
| 输出格式 | 1 到 2 句话，建议 30 到 80 个中文字符 |

固定格式：

~~~text
接收【输入】，完成【动作】，输出【结果】，供【下一节点】继续处理。
~~~

### 7.5 Product rationale

字段名：logicNotes[nodeId].thinking

| 项目 | 说明 |
|---|---|
| 内容含义 | 说明为什么产品需要这个节点，以及这个节点背后的产品判断 |
| 填写要求 | 至少回答“解决什么问题、为什么放在这里、为什么不采用更简单方案”中的两项 |
| 输出格式 | 一段话，建议 2 到 4 句，约 50 到 120 个中文字符 |

可以写：

- 用户在哪一步遇到问题。
- 这个节点如何降低歧义、误操作、失控、等待或不可复核风险。
- 为什么这里需要用户确认。
- 为什么系统自动完成这一环节。
- 这个节点和前后节点如何形成闭环。
- 当前设计牺牲了什么，换来了什么。

不能只写：

~~~text
这里调用了某某 API。
这里使用了某某框架。
这是一个重要节点。
~~~

### 7.6 Case evidence

字段名：logicNotes[nodeId].evidence

| 项目 | 说明 |
|---|---|
| 内容含义 | 证明这个节点在真实产品中存在，并且可以被观察或验证的材料 |
| 填写要求 | 必须指向真实截图、视频时间点、用户输入、工具调用、测试结果、失败样例或状态变化。不能只写“已实现” |
| 输出格式 | 一段证据描述，建议 1 到 3 句；如有多个材料，用“；”分隔 |

推荐格式：

~~~text
证据类型：截图 / 视频时间点 / 测试记录 / 真实对话 / 工具记录。
具体证据：【材料路径或可复现过程】，证明【这个节点的具体行为】。
~~~

### 7.7 节点字段完整格式

每个节点必须按照以下顺序输出：

~~~text
节点 1

节点 ID：
【与 Mermaid 和 logicNotes 对应的唯一 ID】

节点分类：
【label】

节点名称：
【title】

节点说明：
【description】

Product rationale：
【thinking】

Case evidence：
【evidence】
~~~

### 7.8 节点证据不足时的格式

如果暂时没有证据，不要编造：

~~~text
Case evidence：
待补充。当前已确认该节点存在，但还缺少对应截图、视频时间点或可复现测试记录。
~~~

## 八、“从问题到方案”章节字段

当前模板的章节数据使用六个固定章节。每章都要回答不同问题，不能把六章都写成功能说明。

六章顺序固定为：

1. 项目背景
2. 用户问题
3. 产品目标
4. 核心方案
5. 关键决策
6. 评测与结果

### 8.1 章节名称

字段名：chapters[].name

| 项目 | 说明 |
|---|---|
| 内容含义 | 章节属于哪一个阶段 |
| 填写要求 | 使用六个固定章节名称，不要随意改成技术模块名称 |
| 输出格式 | 一行短名称 |

固定格式：

~~~text
章节名称：项目背景
~~~

### 8.2 章节标题

字段名：chapters[].title

| 项目 | 说明 |
|---|---|
| 内容含义 | 本章节最值得记住的核心判断 |
| 填写要求 | 不能只是重复章节名称，要有明确结论或判断 |
| 输出格式 | 一句话，建议 12 到 28 个中文字符 |

示例格式：

~~~text
章节标题：用户需要的不是一句答案，而是一条可复核的证据链。
~~~

### 8.3 章节说明

字段名：chapters[].description

| 项目 | 说明 |
|---|---|
| 内容含义 | 对章节标题进行补充，说明这一章讨论什么 |
| 填写要求 | 用事实和任务范围补充核心判断，不要写宣传口号 |
| 输出格式 | 1 到 2 句，建议 30 到 80 个中文字符 |

### 8.4 章节关注点

字段名：chapters[].focus

| 项目 | 说明 |
|---|---|
| 内容含义 | 本章节的分析重点 |
| 填写要求 | 用一句话指出这一章要抓住的产品判断 |
| 输出格式 | 一句话或一个短语，建议不超过 30 个中文字符 |

示例格式：

~~~text
章节关注点：把用户需求从“更快得到答案”重新定义为“更快完成复核”。
~~~

### 8.5 章节正文

字段名：chapters[].detail

| 项目 | 说明 |
|---|---|
| 内容含义 | 对本章节核心判断的具体推导 |
| 填写要求 | 说明事实、判断和它对下一阶段的影响。不能只列功能，也不能只复述前面的摘要 |
| 输出格式 | 一段话，建议 2 到 4 句，约 80 到 180 个中文字符 |

各章节的正文重点：

| 章节 | 正文必须说明 |
|---|---|
| 项目背景 | 场景是什么，原来如何完成任务，为什么值得解决 |
| 用户问题 | 用户具体卡在哪里，问题造成什么风险或成本 |
| 产品目标 | 希望改变什么，如何判断目标是否达成 |
| 核心方案 | 产品如何从输入走到结果，关键机制如何协同 |
| 关键决策 | 面对哪些方案取舍，为什么选择当前设计 |
| 评测与结果 | 如何验证，观察到什么，当前还有什么限制 |

### 8.6 章节证据

字段名：chapters[].evidence

| 项目 | 说明 |
|---|---|
| 内容含义 | 支撑本章节判断的事实材料 |
| 填写要求 | 每条证据都要能被单独核对，并且要和本章节内容直接相关 |
| 输出格式 | 数组列表，建议每章 2 到 4 条，每条是一条具体材料 |

推荐格式：

~~~text
章节证据：
- 【证据 1】材料路径或事实描述
- 【证据 2】材料路径或事实描述
- 【证据 3】材料路径或事实描述
~~~

不推荐：

~~~text
- 产品做得很好
- 用户反馈不错
- 已经完成开发
~~~

推荐：

~~~text
- 测试记录中，用户连续修改三次条件后仍能回到上一轮结果继续生成。
- 截图显示每次工具调用均有状态卡片，并能展开查看参数和结果。
~~~

### 8.7 单个章节的完整格式

~~~text
### 01 项目背景

章节名称：
项目背景

章节标题：
【本章核心判断】

章节说明：
【对标题的补充说明】

章节关注点：
【本章重点】

章节正文：
【事实、判断和推导】

章节证据：
- 【具体证据 1】
- 【具体证据 2】
~~~

## 九、评测与反馈字段

这一组字段说明如何判断产品方案是否有效，以及当前结果和限制是什么。

### 9.1 关键取舍

字段名：key decisions

| 项目 | 说明 |
|---|---|
| 内容含义 | 产品做了什么选择，放弃了什么选择，为什么这样选择 |
| 填写要求 | 至少写清约束、被比较的方案和最终判断。不要只写“采用了某某技术” |
| 输出格式 | 2 到 4 条列表，每条使用“问题 / 选择 / 原因 / 代价” |

示例格式：

~~~text
关键取舍：
- 在完全自动执行和用户可控之间，选择在高风险节点增加确认，以换取更低的误操作风险。
~~~

### 9.2 评测任务

字段名：evaluation tasks

| 项目 | 说明 |
|---|---|
| 内容含义 | 用来检验产品的真实任务或测试样例 |
| 填写要求 | 任务必须接近产品实际使用，不要只测试“页面能否打开” |
| 输出格式 | 列表，每条写任务输入、任务目标和任务条件 |

### 9.3 评测方法

字段名：evaluation method

| 项目 | 说明 |
|---|---|
| 内容含义 | 通过什么方法判断产品结果好不好 |
| 填写要求 | 说明观察对象、判断标准和记录方式。没有量化指标时，可以使用结构化定性观察 |
| 输出格式 | 一段话或 3 条以内列表 |

### 9.4 观察结果

字段名：observed results

| 项目 | 说明 |
|---|---|
| 内容含义 | 评测中实际观察到的结果 |
| 填写要求 | 区分事实、结论和推断。没有数据时不能写精确比例 |
| 输出格式 | 列表，每条使用“观察到的现象 + 对产品的含义” |

### 9.5 失败样例

字段名：failure cases

| 项目 | 说明 |
|---|---|
| 内容含义 | 产品没有达到预期的真实案例 |
| 填写要求 | 写清输入、预期结果、实际结果和失败位置。失败样例比笼统地写“偶尔出错”更有价值 |
| 输出格式 | 每个案例使用四行：输入、预期、实际、失败位置 |

### 9.6 当前限制

字段名：current limitations

| 项目 | 说明 |
|---|---|
| 内容含义 | 当前版本仍然存在的产品、数据、模型、工具或交互限制 |
| 填写要求 | 只写已经确认的限制，不要为了显得完整而编造问题 |
| 输出格式 | 2 到 5 条列表，每条写限制和影响 |

### 9.7 下一步验证

字段名：next validation

| 项目 | 说明 |
|---|---|
| 内容含义 | 下一轮最需要验证的问题 |
| 填写要求 | 必须是可执行、可观察的验证任务，不要写泛泛的“继续优化” |
| 输出格式 | 列表，每条使用“待验证问题 / 验证方法 / 通过标准” |

## 十、素材清单字段

这一组字段用于告诉后续制作或网页修改环节，哪些材料可以直接使用。

### 10.1 产品截图

字段名：product screenshots

| 项目 | 说明 |
|---|---|
| 内容含义 | 产品界面、关键状态或前后对比截图 |
| 填写要求 | 每张图写清用途，并和逻辑图节点或章节证据建立对应关系 |
| 输出格式 | 路径列表，格式为“路径：用途：对应节点或章节” |

### 10.2 流程图或架构图原稿

字段名：diagram source

| 项目 | 说明 |
|---|---|
| 内容含义 | 原始流程图、白板图、Mermaid 源码或结构草图 |
| 填写要求 | 优先提供可编辑源码或原文件，不要只提供导出的图片 |
| 输出格式 | 文件路径加文件类型和用途 |

### 10.3 演示视频

字段名：demo videos

| 项目 | 说明 |
|---|---|
| 内容含义 | 产品演示视频文件 |
| 填写要求 | 和第四部分的视频路径保持一致，注明每条视频展示的任务 |
| 输出格式 | 路径列表，格式为“路径：展示任务” |

### 10.4 测试记录

字段名：test records

| 项目 | 说明 |
|---|---|
| 内容含义 | 测试任务、运行日志、结果对比或评测表 |
| 填写要求 | 需要能追溯测试输入、预期结果和实际结果 |
| 输出格式 | 文件路径或记录位置，加一句材料说明 |

### 10.5 用户反馈

字段名：user feedback

| 项目 | 说明 |
|---|---|
| 内容含义 | 用户试用时的原话、观察记录或反馈整理 |
| 填写要求 | 区分用户原话和自己的归纳，不要把自己的猜测写成用户反馈 |
| 输出格式 | 反馈来源、反馈内容、对应问题或节点 |

### 10.6 其他可公开材料

字段名：public materials

| 项目 | 说明 |
|---|---|
| 内容含义 | 可以放进公开作品集的补充材料 |
| 填写要求 | 明确材料是否可以公开，涉及隐私、公司信息、密钥或内部数据时不要直接提交 |
| 输出格式 | 路径或链接，加材料用途和公开限制 |

## 十一、待确认字段

### 11.1 尚未确认的事实

字段名：unconfirmed facts

| 项目 | 说明 |
|---|---|
| 内容含义 | Agent 从现有材料中无法确认真伪的内容 |
| 填写要求 | 写出具体事实和为什么无法确认，不要直接删除 |
| 输出格式 | 列表，格式为“待确认事实 / 缺少的材料” |

### 11.2 缺少的证据

字段名：missing evidence

| 项目 | 说明 |
|---|---|
| 内容含义 | 已经形成判断，但目前没有对应的截图、视频、测试或记录 |
| 填写要求 | 说明需要补什么证据，以及证据要证明什么 |
| 输出格式 | 列表，格式为“判断 / 缺少的证据 / 证据用途” |

### 11.3 不能公开的内容

字段名：non-public content

| 项目 | 说明 |
|---|---|
| 内容含义 | 不能出现在公开作品集中的内部信息 |
| 填写要求 | 只列出限制，不要把敏感内容原文复制进模板 |
| 输出格式 | 列表，格式为“内容类型 / 处理方式” |

### 11.4 需要用假设标注的内容

字段名：assumptions

| 项目 | 说明 |
|---|---|
| 内容含义 | 当前用于构思或验证、但还没有真实材料证明的判断 |
| 填写要求 | 必须明确写“假设”，并说明如何验证 |
| 输出格式 | 列表，格式为“假设 / 依据 / 验证方法” |

## 十二、Agent 最终提交格式

其他 Agent 最终必须按照以下顺序返回，不要改变字段名称。字段没有内容时保留字段，并写“待确认”或“无”。

~~~text
# 项目资料整理

## 一、项目定位

项目名称：
项目类别：
目标用户或使用场景：
核心任务：
当前产品状态：
项目重点：
产品范围或任务边界：

## 二、项目概览文案

大标题：
副标题：
项目摘要：
Users：
Status：
Focus：
Scope：

## 三、产品演示材料

视频 1 路径：
视频 1 标题：
视频 1 展示任务：
视频 1 关键结果：

视频 2 路径：
视频 2 标题：
视频 2 展示任务：
视频 2 关键结果：

截图：
- 路径：
  对应状态：
  说明：

## 四、产品逻辑图的定义依据

项目最核心的特点：
如果没有这个特点，产品会退化成什么：
用户输入：
用户任务终点：
用户遇到的主要问题：
必须存在的产品机制：
系统可以自动完成的动作：
必须由用户确认或控制的动作：
失败、回退或重试方式：
最终结果如何反馈给用户：

## 五、产品逻辑图

逻辑图标题：
逻辑图说明：
图形格式：
流程方向：
主流程：
反馈或控制关系：
不在图中展开的内容：

Mermaid 源码：
【粘贴 Mermaid flowchart 源码】

初始选中节点：
选择原因：

## 六、逻辑图节点

节点 1：

节点 ID：
节点分类：
节点名称：
节点说明：
Product rationale：
Case evidence：

节点 2：

节点 ID：
节点分类：
节点名称：
节点说明：
Product rationale：
Case evidence：

按同样格式继续，直到覆盖所有需要解释的关键节点。

## 七、从问题到方案

### 01 项目背景

章节名称：
章节标题：
章节说明：
章节关注点：
章节正文：
章节证据：
- 

### 02 用户问题

章节名称：
章节标题：
章节说明：
章节关注点：
章节正文：
章节证据：
- 

### 03 产品目标

章节名称：
章节标题：
章节说明：
章节关注点：
章节正文：
章节证据：
- 

### 04 核心方案

章节名称：
章节标题：
章节说明：
章节关注点：
章节正文：
章节证据：
- 

### 05 关键决策

章节名称：
章节标题：
章节说明：
章节关注点：
章节正文：
章节证据：
- 

### 06 评测与结果

章节名称：
章节标题：
章节说明：
章节关注点：
章节正文：
章节证据：
- 

## 八、评测与反馈

关键取舍：
评测任务：
评测方法：
观察结果：
失败样例：
当前限制：
下一步验证：

## 九、素材清单

产品截图：
流程图或架构图原稿：
演示视频：
测试记录：
用户反馈：
其他可公开材料：

## 十、待确认内容

尚未确认的事实：
缺少的证据：
不能公开的内容：
需要用假设标注的内容：
~~~

## 十三、交付前检查

Agent 提交前必须逐项检查：

- 每个字段都填写了内容，或明确写“待确认”“无”。
- 大标题写的是用户价值，不是技术名词。
- 副标题写的是实现价值的产品机制。
- 项目摘要交代了产品、用户、任务和状态。
- 逻辑图标题和说明明确写出核心特点。
- Mermaid 源码中的可点击节点 ID 与节点解释一一对应。
- logicInitial 对应一个真实节点 ID。
- 每个节点都包含节点说明、Product rationale 和 Case evidence。
- 六个章节顺序完整，且每章回答不同问题。
- evidence 是可核对的事实材料，不是“已实现”。
- 评测结果区分了观察事实、产品判断和待验证假设。
- 不包含角色、实际工作、团队分工或视频提示词。
- 没有编造数字、用户反馈或上线结果。
- 所有文件路径都是真实存在或明确标注为待补充。
